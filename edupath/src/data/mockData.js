/**
 * EduPath Data Module
 * All application state is dynamically retrieved from PostgreSQL via backend APIs.
 * Hardcoded application mock data has been removed.
 */

export const defaultProfile = {
  name: "",
  targetRole: "",
  experience: "",
  currentSkills: [],
  careerGoal: "",
};

export const initialSkillMatrix = [];
export const initialRoadmap = [];
export const weeklyActivityData = [];
export const practiceChallenges = [];
